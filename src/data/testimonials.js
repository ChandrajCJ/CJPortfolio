/**
 * Recommendations. Intentionally empty - the section only renders when populated.
 * Only add quotes you actually received (e.g. pulled from LinkedIn recommendations).
 *
 * Shape: { quote, name, role, company, url }
 */
export const testimonials = []

export default testimonials
